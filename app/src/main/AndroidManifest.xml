<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PANEL BAĞLANTISI İÇİN İNTERNET İZİNLERİ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- SİSTEM İZİNLERİ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> 

    <application
        android:name=".BoxApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true" 
        android:icon="@drawable/icon"
        android:label="𝒀𝑨𝑯𝒀𝑨 𝑽𝑰𝑷" 
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 1. AÇILIŞ EKRANI -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. GİRİŞ EKRANI (Lisans Kontrolü Burada Yapılacak) -->
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 3. ANA MENÜ -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- HİLE MOTORLARI VE SERVİSLER -->
        <service android:name=".floating.Overlay" android:exported="false" />
        <service android:name=".floating.FloatService" android:exported="false" />
        <service android:name=".floating.ToggleBullet" android:exported="false" />

    </application>
</manifest>
